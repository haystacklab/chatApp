import React, { Component } from 'react'

class AddMessage extends Component {
    render() {
        return (
            <div>
                Add Message
            </div>
        )
    }
}

export default AddMessage;